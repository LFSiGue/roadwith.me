import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const profiles=sqliteTable('profiles',{userId:text('user_id').primaryKey(),nickname:text('nickname').notNull(),phone:text('phone').notNull(),contactName:text('contact_name').notNull().default(''),contactPhone:text('contact_phone').notNull().default('')},t=>[uniqueIndex('profiles_nickname_unique').on(t.nickname)]);
export const trips=sqliteTable('trips',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>profiles.userId),corridor:text('corridor').notNull(),origin:integer('origin').notNull(),destination:integer('destination').notNull(),departure:integer('departure').notNull(),endsAt:integer('ends_at').notNull(),status:text('status').notNull(),support:integer('support').notNull().default(0),emergency:integer('emergency').notNull().default(0),reason:text('reason').notNull().default(''),location:text('location').notNull().default('')},t=>[index('trips_user').on(t.userId),index('trips_status_ends').on(t.status,t.endsAt)]);
export const connections=sqliteTable('connections',{id:text('id').primaryKey(),fromTrip:text('from_trip').notNull().references(()=>trips.id),toTrip:text('to_trip').notNull().references(()=>trips.id),status:text('status').notNull().default('pending'),createdAt:integer('created_at').notNull()},t=>[uniqueIndex('connections_pair').on(t.fromTrip,t.toTrip),index('connections_to').on(t.toTrip)]);
